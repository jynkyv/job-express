# Job Express 工作日志（2026-06-07 至 2026-06-08）

## 一、整体目标

这两天主要围绕 Job Express 的启动稳定性、三入口工作台、产品介绍页和“形象分析”页面做修复与打磨。核心目标是让软件打开后能稳定访问，并让形象分析页面在视觉、文案和交互上更符合当前产品的扁平化风格。

## 二、完成的主要工作

### 1. 修复本地软件无法稳定访问的问题

之前软件多次出现 `http://127.0.0.1:3000/` 无法正常访问、页面打不开、构建后页面资源还是旧版本等问题。排查后发现主要原因是本地服务进程和构建产物之间存在不同步：旧进程占用 3000 端口，页面访问到的仍可能是旧的 `dist` 内容。

处理方式：

- 检查 3000 端口监听进程。
- 结束旧的服务进程，让守护进程重新拉起服务。
- 使用本地构建命令重新生成 `dist`。
- 多次通过浏览器自动化访问页面，确认页面返回 200 且无前端报错。

相关文件：

- `jobexpress-guardian.cjs`
- `open-jobexpress.cmd`
- `open-jobexpress.vbs`
- `server.mjs`

### 2. 明确首页和产品介绍页的关系

最初讨论过“把 Job Express 首页移植为默认首页”，后来明确产品逻辑应为：

- 软件启动后先进入三入口工作台。
- 三个入口分别对应不同产品方向。
- 点击入口后进入各自的产品介绍页，而不是直接把当前首页替换掉。

这部分主要是产品结构判断，避免把“介绍页”和“启动首页/工作台”混为一谈。

### 3. 优化形象分析页面的预览区

原来的照片预览区存在几个问题：

- 外层框和图片圆角不一致，图是圆角但容器像直角，视觉不协调。
- 预览图偏真实证件照风格，和当前软件的扁平、轻量、卡片化风格不统一。
- 示例图容易被误认为真实上传照片。

处理方式：

- 将预览区统一为白色圆角卡片。
- 统一图片容器和图片本身的圆角。
- 替换为更符合参考图风格的 3D 卡通职业头像。
- 保留“职业形象示例”和“上传本人照片后，这里会替换为实际预览”的说明。

相关文件：

- `src/features/career/components/ImageAnalysis.tsx`
- `public/image-analysis/appearance-cartoon.png`

### 4. 重新生成并替换形象分析示例图

第一版新图虽然比真实照片更轻，但整体偏精致插画，不够接近参考图。之后重新生成了更接近参考图的版本：

- 大头近景。
- 柔和 3D 质感。
- 浅绿色背景。
- 底部暗渐变，方便白色说明文字叠加。
- 西装职业形象，但整体更像产品卡片里的友好头像。

最终使用的新图：

- `public/image-analysis/appearance-cartoon.png`

清理时，旧的未引用素材 `public/image-analysis/appearance-preview.png` 可删除。

### 5. 修改右侧步骤编号

右侧流程步骤原本使用中文“一、二、三”或类似样式，视觉上不够产品化，也不符合当前界面的数字节奏。

处理方式：

- 改为 `01 / 02 / 03`。
- 保留步骤状态说明。

相关位置：

- `src/features/career/components/ImageAnalysis.tsx` 中的 `ChecklistItem`

### 6. 修正文案不准确的问题

页面中原有一句话类似“针对通用类应该怎么穿、怎么整理仪容、怎么呈现职业感”。这里的问题是“通用类”用词不准确，容易让人理解为只有通用岗位需要形象建议，其他岗位不需要。

处理方式：

- 改为“目标岗位需要什么样的着装、仪容和职业呈现”。
- 这样不再绑定某个岗位类型，而是面向用户选择的目标岗位。

### 7. 处理“未上传照片却出现得分”的误导

右侧“报告核心维度”原本显示了职业感、仪容整洁等维度的数字分数。用户还没有上传照片时就出现分数，会让人误以为系统已经完成分析。

处理方式：

- 将标题从“报告核心维度”改为“报告结构预览”。
- 去掉假分数和进度条。
- 添加“示例”标签。
- 增加说明：“这里展示的是报告会覆盖的维度，不代表已完成分析。上传照片并生成报告后，下方报告区会显示真实评分和建议。”

### 8. 优化照片拍摄要点卡片

原来四个拍摄要点左侧显示“对”字，看起来突兀，也不清楚含义。

处理方式：

- 改成更明确的建议标签。
- 使用轻量图标和“建议”文字，让用户知道这是拍摄建议，而不是判断结果。

### 9. 恢复并优化年龄、身高、体重的上下调节控件

身高和体重字段原本有上下调节指针，后来在页面重构中变成普通输入框，导致交互退步。后续补回时，第一版箭头像一个带边框的小胶囊，和扁平化界面不搭；第二版虽然去掉边框，但箭头靠在数值旁边，没有贴到卡片右侧，也不好看。

处理方式：

- 新增 `NumberStepper` 组件。
- 年龄、身高、体重统一使用该组件。
- 左侧显示数值和单位。
- 右侧固定显示上下箭头。
- 去掉箭头外框、白底胶囊和分割线。
- 保留悬停反馈。
- 限制合理范围：年龄 16-70，身高 120-220，体重 30-180。

相关文件：

- `src/features/career/components/ImageAnalysis.tsx`

### 10. 构建和页面验证

每轮重要修改后都执行了构建检查：

```bash
npm.cmd run build
```

构建结果通过。Vite 仍提示部分 chunk 体积较大，这是现有项目体积警告，不是本次修改造成的阻断错误。

同时通过本地浏览器访问并截图验证：

- 页面可访问。
- 输入值能正常显示。
- 年龄、身高、体重上下箭头存在。
- 形象分析页面无前端运行错误。

## 三、出现过的问题与修复方式

### 问题 1：软件反复无法访问

原因：本地 3000 端口服务进程和构建产物不一致，旧进程占用端口或服务没有正确重启。

修复：检查端口、结束旧进程、重新构建、重新拉起服务，并用浏览器验证页面。

### 问题 2：示例图风格不匹配

原因：第一版生成图偏插画，不够像用户参考图中的柔和 3D 大头卡片。

修复：重新生成更接近参考图的素材，并替换项目中的 `appearance-cartoon.png`。

### 问题 3：未上传照片却显示分数

原因：右侧维度区域使用了示例分数，但没有明确说明是示例，容易造成误解。

修复：改为“报告结构预览”，去掉分数和进度条，只保留维度说明。

### 问题 4：步骤编号使用中文显得奇怪

原因：中文编号在当前卡片式界面中不够简洁。

修复：改为 `01 / 02 / 03`。

### 问题 5：身高体重上下指针丢失

原因：页面重构时字段被替换为普通输入框。

修复：新增 `NumberStepper` 并接入身高、体重，之后又扩展到年龄。

### 问题 6：第一版步进器不好看

原因：箭头外层有边框和白底，像额外贴在输入框里的小控件；第二版箭头又靠数值太近，没有贴到卡片右侧。

修复：改成左右分栏结构，数值在左、箭头固定在卡片右侧，去除外框。

### 问题 7：PowerShell 中中文正则导致截图脚本报错

原因：部分 Node 脚本通过 PowerShell here-string 执行时，中文正则被编码破坏。

修复：去掉中文正则检查，改为读取输入值和页面错误数量，避免验证脚本本身影响结果。

## 四、当前状态

截至 2026-06-08：

- 形象分析页面已完成本轮视觉和交互调整。
- 软件本地页面可以访问。
- 构建通过。
- 新示例图已替换。
- 年龄、身高、体重上下调节控件已统一。
- 右侧报告预览不再显示误导性假分数。

## 五、后续建议

1. 如果继续优化形象分析页，可以进一步调整“准备状态 40/100”的逻辑，避免用户理解为真实评分。
2. 可以把大体积字体做一次精简或按需加载，否则项目包体积较大。
3. 可以把运行守护脚本整理成正式说明文档，方便换电脑后快速启动。
4. 当前 `node_modules` 和 `dist` 不建议随源码包一起发送，可由接收方使用 `pnpm install` 和 `npm run build` 重新生成。
